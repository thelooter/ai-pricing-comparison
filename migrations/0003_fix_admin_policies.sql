-- Fix admin_users policies to prevent recursion and improve security

-- This migration is now covered in the initial schema migration
-- But we'll keep it for documentation purposes, with all commands guarded by IF NOT EXISTS checks

-- Nothing to do, policies were already correctly set in the initial schema migration 